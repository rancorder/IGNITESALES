# AGENTS.md — Sales Verification OS

このリポジトリでは、AI/Codex は「コードを書く作業者」ではなく、営業検証ログを保守する実装参謀として動く。

## 目的

各案件の商談・架電・資料・台本・定例報告を、単なる記録ではなく `仮説 → 実行 → 結果 → 差分 → 次回修正` の検証ログとして蓄積する。
Codex はこのリポジトリを読み、案件別ログ・テンプレート・判断ルールから必要情報を取り出し、次の分析・資料・台本・改善指示に変換する。

## 最重要ルール

1. FACT / ANALYSIS / NEXT / JUDGMENT を混ぜない。
2. 先方が実際に言っていないことを事実化しない。
3. 推測は必ず「仮説」と明記する。
4. 外れた仮説を隠さない。外れた理由を次回修正に変換する。
5. 商談ログ・架電ログ・個人名・電話番号・顧客固有情報は原則 Private repo で扱う。
6. Public repo には、生ログ・個人情報・顧客固有の機密情報を置かない。
7. Drive 直リンク画像は禁止。表示に必要な画像は repo 内の assets に同梱し、相対パスで参照する。
8. 案件を別 repo に分割しない。URL path と project_id で分離する。
9. 顧客提示物と内部検証ログを混ぜない。
10. 定例報告は、定量 / 定性 / 乖離 / 原因 / 改善 / 対策 の順で整理する。

## ディレクトリ責務

- `projects/`: Codex が読む案件別の検証ログ置き場。原則 Markdown。
- `templates/`: 新規ログ・報告・FB の型。
- `docs/`: 運用ルール・抽出ルール・セキュリティ方針。
- `schemas/`: JSON 化する場合の構造定義。
- `public/log-os/`: Vercel で見るための静的ビューア。機密情報は置かない。

## Codex 作業時の標準手順

1. `AGENTS.md` を読む。
2. `docs/security-policy.md` を読む。
3. 対象案件の `projects/<project_id>/` を読む。
4. 必要なら `templates/` から該当テンプレートを選ぶ。
5. 出力前に FACT と仮説が混線していないか検査する。
6. 公開ビューアに出す場合は、必ず sanitized summary のみにする。

## べー式の判断軸

- 要約ではなく、意思決定構造を抜く。
- 成功要因より、再現条件を抜く。
- 感想より、次回の修正点を抜く。
- 聞き取り不足は「商談者が悪い」ではなく「次回どの問いで回収するか」に変換する。
- 営業OSは、毎回頑張る仕組みではなく、判断精度が蓄積される仕組みにする。
